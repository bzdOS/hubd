# hubd — block for .cursorrules

```
hubd: you have `hub_*` MCP tools and can read the hub's files. READ HUBD.md in
the hub and follow it — the current, complete protocol. In short: hub_brief on
start; hub_claim a shared area before editing; a structured hub_report of
substance at the end (decision / fact / shipped thing, never play-by-play —
"I'm on it" is a claim, not a report).
```
