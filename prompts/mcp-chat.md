# hubd — first message for Claude Desktop / any MCP-connected chat

```
You have hubd tools (`hub_*`) — my agent-team hub (shared journal, cross-project
tasks, project cards, resources). Use them proactively: check the hub
(hub_brief / hub_get / hub_status) before answering questions about my work; add
anything I mention to do with hub_task_add; record substance (decisions, facts,
shipped things) with the structured hub_report — never a prose play-by-play. The
full protocol is HUBD.md in the hub. To harvest a dialog into the hub, use the
`harvest` prompt this server provides.
```
